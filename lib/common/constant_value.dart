class ConstantValue {
  static String stockMomentumFileName =
      'stock_momentum_${DateTime.now().toString().substring(0, 10)}';
  static String canaryMomentumFileName =
      'cnanry_momentum_${DateTime.now().toString().substring(0, 10)}';
}
